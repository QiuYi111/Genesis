# @package _global_
model:
  provider: null
  api_key_env: "NULL_API_KEY"  # Not used but required for compatibility
  agent_model: "null-model"
  trinity_model: "null-model"
  base_url: "null"
  
  # Temperature settings (not used but required for compatibility)
  temperatures:
    agent_action: 0.0
    agent_goal: 0.0
    trinity_adjudicate: 0.0
    trinity_execute: 0.0
    world_rules: 0.0
  
  # Retry settings (not used but required for compatibility)
  max_retries: 0
  retry_delay: 0.0